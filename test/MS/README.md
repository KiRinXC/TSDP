# MS 测试

本目录保存尚未进入 `lab/` 或正式 `exp/MS/` 的 Model Stealing 方法测试。编号目录
用于固定每项测试的独立问题、输入和输出；测试结果统一写入
`results/test/MS/<编号_关键词>/`，不写入正式 MS 索引，也不覆盖既有 Lab 结果。

当前测试：

```text
01_cross    ResNet18 的 40/16 项残差与有效秩，以及 Product 前缀 MS 诊断
```
